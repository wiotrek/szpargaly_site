image: node:12.16.2
pages:
  cache:
    paths:
      - node_modules/
  script:
    - npm install -g @angular/cli@9.1.1
    - npm install
    - npm run buildProd
    - lftp -e "open mkwk019.cba.pl; user $FTP_USERNAME $FTP_PASSWORD; mirror -X .* -X .*/ --reverse --verbose --delete local-folder/ destination-folder/; bye"
  artifacts:
    paths:
      - public
  only:
    - master
    - pages